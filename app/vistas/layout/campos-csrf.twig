<input type="hidden" name="{{csrf.keys.name}}" value="{{csrf.name}}" class="csrf-key-name" ref="csrfname">
<input type="hidden" name="{{csrf.keys.value}}" value="{{csrf.value}}" class="csrf-key-value" ref="csrfvalue">